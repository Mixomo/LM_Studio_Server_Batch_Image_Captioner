[project]
name = "llamacpp-native-batch-captioner"
version = "0.1.0"
description = "Minimal CustomTkinter batch image captioner for llama-cpp-python."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "customtkinter>=5.2.2",
]

[tool.uv]
package = false
